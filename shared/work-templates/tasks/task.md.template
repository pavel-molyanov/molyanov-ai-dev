---
status: planned                    # planned -> in_progress -> done
depends_on: []                     # dependency task numbers
wave: 1                            # parallel execution wave
skills: [code-writing]             # ARRAY of skills to load
verify: []                         # verification types: smoke, user (optional)
reviewers: [code-reviewer, security-auditor, test-reviewer]  # set explicitly. Empty = fallback to the default three
teammate_name:                     # cosmetic teammate name for agent teams (e.g.: coder-inbox, qa-runner, deployer)
---

# Task N: Name

## Required Skills

Before executing the task, load:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

What we're doing and why. (As much as needed for understanding — no length limit.)

## What to do

Concrete steps — WHAT, not HOW. Not pseudocode.

## TDD Anchor

<!-- Fill if task includes writing code. For non-code tasks (user instructions, deploy, config) — delete this section. -->

Tests to write BEFORE implementation. Write → run → confirm they fail → write code → confirm they pass.

- `tests/test_api.py::test_create_user` — POST /api/users creates a user, returns 201
- `tests/test_api.py::test_create_user_duplicate` — POST with an existing email → 409

## Acceptance Criteria

- [ ] Criterion 1
- [ ] Criterion 2

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [architecture.md](~/.claude/skills/project-knowledge/architecture.md)
- [patterns.md](~/.claude/skills/project-knowledge/patterns.md)
- [project.md](~/.claude/skills/project-knowledge/project.md)
- [testing.md](~/.claude/skills/project-knowledge/testing.md)
- [other files as needed — DB, API, deploy, specific code files]

## Verification Steps

### Automated
<!-- Tests to run. Always present for code tasks. -->
- `pytest tests/test_xxx.py -v` → all pass

### Smoke
<!-- Executable checks — agent runs before reviews. No deployment needed.
     From tech-spec Verify-smoke. Types: command (curl, python -c), MCP tool,
     API call, docker build, local server check, agent with test prompt.
     Omit section if no smoke checks. -->

### User
<!-- Agent asks user to verify (UI, behavior, experience).
     From tech-spec Verify-user. Omit section if no user checks. -->

## Details

<!-- All details for task execution — technical, organizational, any other. -->

**Files:** path/to/file.ts — what to do
**Dependencies:** other tasks, packages
**Edge cases:** list
**Implementation hints:** hints (NOT pseudocode)

## Reviewers

- **code-reviewer** → `work/{feature}/logs/working/task-{N}/code-reviewer-{round}.json`
- **security-auditor** → `work/{feature}/logs/working/task-{N}/security-auditor-{round}.json`
- **test-reviewer** → `work/{feature}/logs/working/task-{N}/test-reviewer-{round}.json`

## Post-completion

- [ ] Write a brief report in decisions.md per the template (Summary: 1-3 sentences, reviews with links to JSON, no findings tables or dumps)
- [ ] If you deviated from the spec — describe the deviation and the reason
- [ ] Update user-spec/tech-spec if anything changed
