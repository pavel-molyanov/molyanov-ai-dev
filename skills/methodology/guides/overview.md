# AI-First Development: Project Overview

Добро пожаловать в документацию по AI-First методологии разработки!

### Все документы

- 🗂️ **[Folder Structure](folder-structure.md)** - структура глобальной и локальной папок
- 🆕 **[Новый проект](workflows/new-project.md)** - создание проекта с нуля
- 🔄 **[Онбординг](workflows/onboarding.md)** - миграция существующего проекта
- ⚡ **[Разработка](workflows/feature-dev.md)** - ежедневная работа над задачами

---

## Что создаем

AI-first методология разработки для предпринимателей и малых команд. Система работы от идеи до production, где AI-агенты понимают контекст проекта между сессиями.

## Решаемые проблемы

1. **Потеря контекста** - агент забывает что делали вчера
2. **Хаотичная документация** - огромный CLAUDE.md забивает context window
3. **Устаревшие знания** - knowledge cutoff агента (Январь 2025)
4. **Нет структуры** - каждый проект организован по-своему
5. **Проблемы с качеством** - нет систематического review
6. **Сложный onboarding** - новый разработчик теряется

## Решение

**Распределенная Knowledge Base** - модульная структура вместо монолита

**Правильная иерархия** - Context (описание проекта) → User Spec (что хотим) → Tech Spec (как реализовать) → Tasks (конкретная работа)

**MCP Context7** - актуальная документация технологий в реальном времени

**Agent Orchestration** - специализированные субагенты для разных задач

**Автоматизация качества** - code review, тесты, security scanning

**Git-friendly документация** - версионируется вместе с кодом

## Ключевые концепции

### Just-In-Time Context
Агент читает только нужную информацию для текущей задачи, не весь контекст.

### Single Source of Truth
Каждая информация в одном месте, остальные ссылаются на нее.

### Spec-Driven Development
Сначала спецификация, потом код:
1. **Context** - описание проекта (создается один раз, обновляется) [English]
2. **User Spec** - что хотим и почему (для человека) [Русский]
3. **Tech Spec** - как реализовать технически (для агента) [English]
4. **Tasks** - конкретные задачи из Tech Spec [English]
5. **Code** - реализация

### Progressive Disclosure
Информация раскрывается постепенно: от общего к частному.

### Agent Orchestration
Главный агент координирует специализированных субагентов (code-developer, code-reviewer, security-auditor) и использует Skills (testing, infrastructure) для автоматизации задач. Безопасность кода обеспечивается через pre-commit hooks (gitleaks).

### Git-Friendly Documentation
Документация живет в Git вместе с кодом. Reasoning живет в контексте (в описаниях компонентов), история в Git достаточна.

## Выбор workflow

**Новый проект?** → [workflows/new-project.md](workflows/new-project.md)
- Запускаете проект с нуля
- Команды: `/init-project` → `/init-project-discovery` → `/init-context` → `/setup-infrastructure`

**Существующий проект?** → [workflows/onboarding.md](workflows/onboarding.md)
- Есть legacy код без документации
- Команды: `/old-project` → `/old-folder-audit` → `/init-project-discovery` → `/init-context-old` → `/setup-infrastructure-old`

**Разработка фичи/бага?** → [workflows/feature-dev.md](workflows/feature-dev.md)
- Ежедневная работа над задачами
- Команды: `/new-feature` → `/create-tech-spec` → `/tech-spec-decompose` → `/start-task` или `/start-feature`

## Структура Knowledge Base

Методология использует **распределенную систему контекста** вместо монолитного CLAUDE.md:

### Две структуры

1. **Глобальная папка `~/.claude/`** - мета-проект с методологией
   - `shared/templates/` - шаблоны проектов
   - `skills/` - methodology, testing, infrastructure, command-manager (с гайдами и правилами)
   - `agents/` - code-developer, code-reviewer, security-auditor
   - `commands/` - 19 slash-команд для всех workflows
   - `CLAUDE.md` - глобальные инструкции (влияют на ВСЕ проекты!)

2. **Локальная папка проекта** - конкретный проект
   - `.claude/skills/project-knowledge/guides/` - 7 context файлов проекта
   - `work/` - фичи/баги с User Spec, Tech Spec, Tasks
   - `CLAUDE.md` - инструкции конкретного проекта

**Детальное описание:** [folder-structure.md](folder-structure.md)

### Шаблоны проектов

**`~/.claude/shared/templates/new-project/`** - полная структура проекта, копируется через `/init-project`:

```
new-project/
├── .claude/skills/project-knowledge/guides/        # 7 context файлов (templates)
│   ├── project.md          # Описание проекта
│   ├── architecture.md     # Tech stack
│   ├── database.md         # Database config
│   ├── deployment.md       # Deployment setup
│   ├── ux-guidelines.md    # UI/UX guidelines
│   ├── patterns.md         # Code patterns
│   └── git-workflow.md     # Git strategy
├── work/                   # Work items folder
│   └── templates/          # Шаблоны для User Spec, Tech Spec, Tasks
├── .gitignore              # Security-focused
├── .env.example            # Env template
├── CLAUDE.md               # Project instructions
└── README.md               # Project README
```

**`~/.claude/shared/templates/infrastructure/`** - инфраструктурные файлы для `/setup-infrastructure`:
- Pre-commit hooks (gitleaks для security)
- Test templates (smoke tests)
- Docker templates
- CI/CD templates (GitHub Actions)

**`~/.claude/shared/templates/old-folder-audit.md`** - шаблон для аудита legacy кода при миграции

## Workflow для фичи/бага/рефакторинга

```
Идея ("Хочу платежи" / "Баг с дублированием")
    ↓
Интервью (Agent задает вопросы)
    ↓
User Spec (Agent создает)
    ↓
User утверждает
    ↓
Tech Spec (Agent создает)
    ↓
User утверждает
    ↓
Декомпозиция на Tasks (Agent декомпозирует)
    ↓
User утверждает
    ↓
Реализация (TDD + Context7 + code review)
    ↓
Deploy
```

## Context7 (актуальная документация)

**Проблема:** Knowledge cutoff агента → устаревшие API и паттерны

**Решение:** Context7 MCP сервер от Upstash подтягивает актуальную документацию прямо в контекст

**Как работает:**
- Agent добавляет "use context7" в промпт при необходимости
- Context7 фетчит актуальные docs с официальных сайтов
- Agent использует свежую информацию

**Интеграция:** Указывается в `.claude/skills/project-knowledge/guides/architecture.md` (одна строка о доступности)

## Структура проекта

Все необходимые папки и файлы уже созданы в этом проекте:

**Документация:**
- `guides/` - руководства по процессам разработки
- `.claude/skills/project-knowledge/guides/` - контекст проекта для агентов
- `.claude/rules/` - правила и чеклисты для агентов

**Рабочие файлы:**
- `work/` - активные фичи/баги с specs и tasks

**Кастомизация:**
- `agents/` - проектные субагенты (по необходимости)
- `commands/` - проектные команды (по необходимости)
- `hooks/` - проектные хуки (по необходимости)

**Детальное описание:** [folder-structure.md](folder-structure.md)
